// A function that concatenates strings without using `+`, `.concat()` and `.join()`
// There must be a space between the two strings

const joinStrings = (string1, string2) => `${string1}  ${string2}`;

console.log(joinStrings("Hello", "World"));
